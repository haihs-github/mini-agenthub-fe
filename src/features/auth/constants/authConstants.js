// BKAV HaiHS : Định nghĩa các hằng số dùng chung cho module Authentication - start
export const SUB_CONFIRM_TYPE = Object.freeze({
  SAVE: "save",
  CANCEL: "cancel",
  SUCCESS: "success",
});

export const EMAIL_REGEX = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
